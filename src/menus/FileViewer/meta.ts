import { FileOutlined } from '@ant-design/icons';
import type { MenuMeta } from '../../types/menu';

const meta: MenuMeta = {
  order: 2,
  title: 'File Viewer',
  icon: FileOutlined
};

export default meta;
