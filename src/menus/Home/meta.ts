import { HomeOutlined } from '@ant-design/icons';
import type { MenuMeta } from '../../types/menu';

const meta: MenuMeta = {
  order: 0, // 最先显示
  title: 'Home', // 可选：自定义标题
  icon: HomeOutlined // 可选：菜单图标
};

export default meta;
